# H2O: Refill Hero — инструкции для AI-агентов

## Ветка разработки

Все изменения делай в ветке **beta**. Не создавай новых feature-веток — работай только в `beta`. При необходимости переключись на неё: `git checkout beta`.

## Релизы

При каждой правке кода см. **RELEASE.md** — поднимай версию и заполняй changelog.
