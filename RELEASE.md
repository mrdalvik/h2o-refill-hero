# Релизы — инструкции для агентов

При любом изменении кода:

1. **Подними версию** в `package.json` на 0.0.1 (например: 0.1.35 → 0.1.36).

2. **Changelog — согласование с пользователем**:
   - После внесения правок предложи пользователю текст для changelog.
   - Пользователь корректирует и согласовывает — только после этого вноси в `src/constants/changelog.ts`.
   - Не пиши в changelog внутренние изменения (beta-флоу, CI/CD, настройки проекта и т.п.) — только то, что интересно пользователям.
   - Формат: эмодзи 🆕 — новое, ⚒️ — исправления. Кратко и понятно.

Пример предложения:
```
Предлагаю для changelog: ['🆕 Добавлена тёмная тема', '⚒️ Исправлен баг с напоминаниями']
```
