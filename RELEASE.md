# Релизы — инструкции для агентов

При любом изменении кода:

1. **Подними версию** в `package.json` на 0.0.1 (например: 0.1.35 → 0.1.36).

2. **Обнови changelog** в `src/constants/changelog.ts`:
   - Добавь новый блок с текущей версией в начало массива `CHANGELOG`.
   - Используй эмодзи: 🆕 — новое, ⚒️ — исправления.
   - Пиши кратко и понятно для пользователя.

Пример:
```
{ version: '0.1.36', entries: ['🆕 Добавлена тёмная тема', '⚒️ Исправлен баг с напоминаниями'] }
```
